{% extends 'base.html.twig' %}

{% block body %}
    <div id="app">
    </div>
    <div class="container-fluid">

        <div class="row">
            <div class="col-xs-12">
                <h3 class="text-center">
                    C'est quoi la suite ???
                </h3>
            </div>
        </div>

        {% if ok == '0' %}
            <div class="row">
                <div class="col-xs-12 text-center">
                    Ton truc, ça donne que dalle ! Essaye autre chose ... Tu peux le faire !
                </div>
            </div>
        {% endif %}

        <div class="row">
            <div class="col-xs-3 text-right">
                Le début
            </div>
            <div class="col-xs-6">
                <form action="{{ path('page2') }}" method="POST">
                    <input id="toFind" name="toFind" class="form-control" type="text" value="{{ toFind }}"><br />
                    <div class="col-xs-12 text-center">
                        <input type="submit" value="Valider">
                    </div>
                </form>

            </div>
        </div>

    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
