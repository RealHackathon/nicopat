{% extends 'base.html.twig' %}

{% block body %}

    {#{{ dump(songs) }}#}

    <ul>
        {% for song in songs %}
            <li>{{ song.Artist }} / {{ song.Song }}</li>
        {% endfor %}
    </ul>

    <hr>

    <p>{{ lyric }}</p>
     {{ dump(lyric) }}

{% endblock %}

{% block stylesheets %}
{% endblock %}
